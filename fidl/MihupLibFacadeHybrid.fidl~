package commonapi

interface MihupLibFacadeHybrid {
  version {major 1 minor 0}
  method MihupLibFacadeHybrid (
      int samplingRate, int bufferSize, short bits_per_sample,String outfileName, String modelDir, Void (*callbackFn)(string),String log_d,String car_id,String rampath)->
    out
      
    }
  }
}
